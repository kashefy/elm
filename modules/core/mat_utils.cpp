#include "core/mat_utils.h"

#include <iostream>

void sem::CumSum(const MatF &src, MatF &dst)
{
//    std::cout<<"cumsum()"<<std::endl;
//    float *src_ptr = reinterpret_cast<float*>(src.data);
//    float *dst_ptr = reinterpret_cast<float*>(dst.data);
//    float interm_sum = 0;
//    for (int i = 0; i < src.total(); i++, ++src_ptr, ++dst_ptr) {

//        interm_sum += *src_ptr;
//        std::cout<<i<<" "<<interm_sum<<std::endl;
//        *dst_ptr = interm_sum;
//    }
}
